/**
 * Placeholder for the main tracking app — fully built out in Phase 6.
 *
 * For now it only confirms that the React Router wiring + post-setup
 * navigation work end-to-end.
 */
export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center p-6">
      <div className="text-center">
        <h1 className="text-2xl font-semibold">Tracker</h1>
        <p className="text-sm text-neutral-400 mt-2">
          Main app — Phase 6
        </p>
      </div>
    </main>
  );
}
