# Dependencies
node_modules/
target/

# Build outputs
dist/
src-tauri/target/
src-tauri/gen/
.vite/

# Editor
.vscode/
.idea/
.claude/
*.swp
.DS_Store

# Env / secrets
.env
.env.local
# Tracker's on-disk secret store. It only ever lives in the per-user app data
# directory on real installs, but this defensive ignore prevents an accidental
# commit if it ever surfaces inside the working tree.
secret.toml

# Logs
*.log

# OS
Thumbs.db

# Test
coverage/
