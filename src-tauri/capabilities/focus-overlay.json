{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "focus-overlay",
  "description": "Focus mode banner. Click-through, non-interactive, and short-lived: it listens for one event and renders a line of text. It has no business reaching the updater, the process plugin, autostart, notifications or the opener, so it gets the core set and nothing else.",
  "windows": ["focus-overlay"],
  "permissions": ["core:default"]
}
